import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#ff725a",
        },
        secondary: {
            main: "#099df5",
        },
        success: {
            main: "#1adb79",
        },
        error: {
            main: "#d42323",
        }
    }
})